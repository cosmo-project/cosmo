/* import expect from 'expect';
import consoleLogReducer from '../reducer';
import { fromJS } from 'immutable';

describe('consoleLogReducer', () => {
  it('returns the initial state', () => {
    expect(consoleLogReducer(undefined, {})).toEqual(fromJS({}));
  });
});
*/
